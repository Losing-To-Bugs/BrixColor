import {StyleSheet} from "react-native";

const buttonStyles = StyleSheet.create({
    placeholder: {
        borderStyle: 'solid',
        borderWidth: 2,
        padding: 4,
        borderColor: 'white'
    }
})

export default buttonStyles